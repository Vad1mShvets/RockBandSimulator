public enum LocationType
{
    Garage = 0,         // Main base
    Shop = 1,           // Mini-store
    Bar = 2,            // Bar
    Casino = 3,         // Casino
    EnemyGarage = 4,    // Enemy garages (quest targets)
    Street = 5,         // Street (daily events)
    Warehouse = 6,      // Warehouse (fetch quests)
}
